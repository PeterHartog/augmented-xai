# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /load: ames.yaml

save_path: data/ames_cleaned.csv
save_original_path: data/ames_original.csv

clean:
  input_columns: "Drug"
  remove_stereo: false
